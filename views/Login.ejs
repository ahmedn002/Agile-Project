<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@200&display=swap");

      * {
        box-sizing: border-box;
      }

      body {
        background: #131315;
        font-family: "Nunito Sans", sans-serif;
      }

      .login-container {
        border: 1px solid white;
        position: absolute;
        top: 50%;
        left: 50%;
        width: 400px;
        padding: 40px;
        transform: translate(-50%, -50%);
        background-color: #242026;
        box-shadow: 0 15px 25px rgba(221, 89, 239, 0.7);
        border-radius: 10px;
      }

      .registration-container {
        border: 1px solid white;
        position: absolute;
        top: 50%;
        left: 50%;
        width: 400px;
        padding: 40px;
        transform: translate(-50%, -50%);
        background-color: #242026;
        box-shadow: 0 15px 25px rgba(221, 89, 239, 0.7);
        border-radius: 10px;
      }

      h2 {
        margin: 0 0 30px;
        padding: 0;
        color: white;
        text-align: center;
      }

      .user-box input {
        position: relative;
        width: 100%;
        padding: 10px 0;
        font-size: 16px;
        color: white;
        margin-bottom: 30px;
        border: none;
        border-bottom: 1px solid white;
        outline: none;
        background: transparent;
      }

      .user-box label {
        position: relative;
        left: 0;
        top: -60px;
        padding: 10px 0;
        font-size: 16px;
        color: white;
        pointer-events: none;
        transition-duration: 0.5s;
      }

      .user-box input:focus ~ label,
      .user-box input:valid ~ label {
        top: -85px;
        left: 0;
        color: rgba(221, 89, 239, 0.7);
        font-size: 12px;
      }

      #submit {
        padding: 10px 20px;
        color: #cbbddb;
        font-size: 16px;
        text-transform: uppercase;
        overflow: hidden;
        transition: 0.5s;
        letter-spacing: 4px;
        border: 1px solid rgba(221, 89, 239, 0.7);
        background: transparent;
        border-radius: 5px;
      }

      #submit:hover {
        transition: 0.5s;
        background: rgba(221, 89, 239, 0.7);
        color: #fff;
        box-shadow: 0 0 5px rgba(221, 89, 239, 0.7),
          0 0 25px rgba(221, 89, 239, 0.7), 0 0 50px rgba(221, 89, 239, 0.7),
          0 0 100px rgba(221, 89, 239, 0.7);
      }

      .button-form {
        display: flex;
        flex-direction: row;
        margin-top: 20px;
      }

      #register {
        display: flex;
        flex-direction: column;
        font-size: 14px;
        color: #cbbddb;
        width: 60%;
        text-align: center;
      }
      #register a {
        color: rgba(221, 89, 239, 0.7);
        text-decoration: none;
      }

      #registerdiv {
        text-align: right;
      }

      #registerdiv input {
        color: rgba(221, 89, 239, 0.7);
        text-decoration: none;
        padding: 5px 10px;
        border: 1px solid rgba(221, 89, 239, 0.7);
        border-radius: 10px;
        background: transparent;
      }

      #registerdiv input:hover {
        transition: 0.5s;
        background: rgba(221, 89, 239, 0.7);
        color: #fff;
        box-shadow: 0 0 5px rgba(221, 89, 239, 0.7),
          0 0 25px rgba(221, 89, 239, 0.7), 0 0 50px rgba(221, 89, 239, 0.7),
          0 0 100px rgba(221, 89, 239, 0.7);
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <h2>Login</h2>
      <form action="/api/auth/signin" method="POST">
        <div class="user-box">
          <input type="text" name="username" required />
          <label>Username / Phone Number</label>
        </div>
        <div class="user-box">
          <input type="password" name="password" value="" required />
          <label>Password</label>
        </div>

        <div class="button-form">
          <input type="submit" id="submit" value="Submit" />
          <div id="register">
            Don't have an account ?
            <a href="/register"> Register</a>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
